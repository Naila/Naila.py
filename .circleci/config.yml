version: 2.1
jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: cimg/python:3.13.1

    steps:
      - checkout

#      - restore_cache:
#          key: deps9-{{ .Branch }}-{{ checksum "Pipfile.lock" }}

      - run:
          name: Set up pipenv and install requirements
          command: |
            sudo pip install pipenv
            pipenv install

#      - save_cache:
#          key: deps9-{{ .Branch }}-{{ checksum "Pipfile.lock" }}
#          paths:
#            - ./venv

      - run:
          name: Compile files
          command: |
            pipenv run python -m compileall main.py modules common -fq
